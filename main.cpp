#include <iostream>
#include <fstream>
#include <vector>
#include <cstring>
#include "Operation.h"
#include "Factory.h"
#include "Parser.h"
#include "Variable.h"

int main(int argc, char* argv[]) {

  /*
  std::ifstream input_file;
  std::string str;
  input_file.open(argv[1]);
  while (input_file.good()) {
    std::getline(input_file, str, '\n');
    if (!str.empty()) {
      //parser->parse_line(str);
    }
  }
  */

  /*
  operations.push_back(Mapper::Operations["ADD"]->clone());
  operations.push_back(Mapper::Operations["SUB"]->clone());
  operations.push_back(Mapper::Operations["ADD"]->clone());
  operations[0]->execute();
  operations[1]->execute();
  operations[2]->execute();
  */
  return 0;
}
